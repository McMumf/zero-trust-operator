---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tunnelconnections.tunnel.mcmumf.dev
spec:
  group: tunnel.mcmumf.dev
  names:
    kind: TunnelConnection
    listKind: TunnelConnectionList
    plural: tunnelconnections
    singular: tunnelconnection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TunnelConnection is the Schema for the tunnelconnections API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TunnelConnectionSpec defines the desired state of TunnelConnection.
            properties:
              tunnelDns:
                type: string
              tunnelEmail:
                type: string
              tunnelToken:
                type: string
            type: object
          status:
            description: TunnelConnectionStatus defines the observed state of TunnelConnection.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
